<UserControl x:Class="ManagementHouseFee.Views.AddRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:AddRecordViewModel/>
    </UserControl.DataContext>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="📅 날짜 선택 : " VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="80" Margin="10,0"/>
                <TextBlock Text="년" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="60" Margin="10,0"/>
                <TextBlock Text="월" VerticalAlignment="Center"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding InputItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Name}" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold"/>

                                    <TextBox Grid.Column="1" 
                                             Text="{Binding Amount, StringFormat={}{0:N0}, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="16" HorizontalContentAlignment="Right" Padding="5"
                                             GotFocus="TextBox_GotFocus"
                                             PreviewMouseLeftButtonDown="TextBox_PreviewMouseLeftButtonDown"/>

                                    <TextBlock Grid.Column="2" Text=" 원" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="⚙ 항목 관리" Command="{Binding ManageItemsCommand}" Width="100" Height="40" Background="Gray" Foreground="White" Margin="0,0,10,0"/>
                <Button Content="💾 저장하기" Command="{Binding SaveCommand}" Width="120" Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="#FAFAFA" Margin="10" CornerRadius="10">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="실시간 비율 분석" FontSize="18" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Left"/>
                    <TextBlock Text="{Binding TotalAmountText}" FontSize="18" FontWeight="Bold" Foreground="#007ACC" HorizontalAlignment="Right"/>
                </Grid>

                <lvc:PieChart Grid.Row="1" Series="{Binding PieSeries}" LegendLocation="Bottom" InnerRadius="50">
                    <lvc:PieChart.ChartLegend>
                        <lvc:DefaultLegend BulletSize="10"/>
                    </lvc:PieChart.ChartLegend>
                </lvc:PieChart>
            </Grid>
        </Border>
    </Grid>
</UserControl>