<UserControl x:Class="ManagementHouseFee.Views.ItemComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ItemComparisonViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔎 항목별 상세 분석" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,30,0"/>

                <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" FontSize="16"/>
                <TextBlock Text="년" VerticalAlignment="Center" Margin="5,0,15,0" FontSize="16"/>

                <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="80" FontSize="16"/>
                <TextBlock Text="월" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="16"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="20">
            <ItemsControl ItemsSource="{Binding ComparisonList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="10" Margin="0,0,0,15" Padding="20">
                            <Border.Effect>
                                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.1"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="이번 달" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding CurrentAmount, StringFormat={}{0:N0}원}" FontSize="18" FontWeight="SemiBold"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding PrevMonthMessage}" 
                                               Foreground="{Binding PrevMonthColor}" 
                                               FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>

                                    <TextBlock Text="{Binding PrevYearMessage}" 
                                               Foreground="{Binding PrevYearColor}" 
                                               FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>