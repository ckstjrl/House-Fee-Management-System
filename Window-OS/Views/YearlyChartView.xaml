<UserControl x:Class="ManagementHouseFee.Views.YearlyChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
             xmlns:local="clr-namespace:ManagementHouseFee.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:YearlyChartViewModel/>
    </UserControl.DataContext>

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📅 연도별 월평균 관리비 비교" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333"/>

        <lvc:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}" LegendLocation="Right">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="연도" Labels="{Binding Labels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="평균 금액 (원)" LabelFormatter="{Binding Formatter}"/>
            </lvc:CartesianChart.AxisY>

            <lvc:CartesianChart.DataTooltip>
                <local:CustomTooltipView SelectionMode="SharedYValues"/>
            </lvc:CartesianChart.DataTooltip>

        </lvc:CartesianChart>
    </Grid>
</UserControl>