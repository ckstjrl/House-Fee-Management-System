<UserControl x:Class="ManagementHouseFee.Views.CustomTooltipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             
             mc:Ignorable="d" 
             Background="Transparent">

    <Border Background="#F5FFFFFF" 
            BorderBrush="#DDDDDD" 
            BorderThickness="1" 
            CornerRadius="5" 
            Padding="10">

        <Border.Resources>
            <DataTemplate DataType="{x:Type wpf:DataPointViewModel}">
                <Grid Margin="5,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Title"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Value"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Percent"/>
                    </Grid.ColumnDefinitions>

                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{Binding Series.Fill}" VerticalAlignment="Center" Margin="0,0,5,0"/>

                    <TextBlock Grid.Column="1" Text="{Binding Series.Title}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <TextBlock Grid.Column="2" Text="{Binding ChartPoint.Y, StringFormat={}{0:N0}}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"/>

                    <TextBlock Grid.Column="3" Text="{Binding ChartPoint.Participation, StringFormat={}{0:P1}}" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#666666"/>
                </Grid>
            </DataTemplate>
        </Border.Resources>

        <ItemsControl ItemsSource="{Binding Data.Points}" Grid.IsSharedSizeScope="True">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Border>
</UserControl>