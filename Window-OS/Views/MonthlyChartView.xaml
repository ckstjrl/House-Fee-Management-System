<UserControl x:Class="ManagementHouseFee.Views.MonthlyChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
             xmlns:local="clr-namespace:ManagementHouseFee.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:MonthlyChartViewModel/>
    </UserControl.DataContext>

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="📊 월별 추이 분석" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" FontSize="16" VerticalContentAlignment="Center"/>
            <TextBlock Text="년 기준" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="16"/>
        </StackPanel>

        <lvc:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}" LegendLocation="Right">

            <lvc:CartesianChart.AxisX>
                <lvc:Axis Labels="{Binding Labels}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="1" StrokeThickness="0"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>

            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="금액 (원)" LabelFormatter="{Binding Formatter}"/>
            </lvc:CartesianChart.AxisY>

            <lvc:CartesianChart.DataTooltip>
                <local:CustomTooltipView SelectionMode="OnlySender" />
            </lvc:CartesianChart.DataTooltip>

        </lvc:CartesianChart>
    </Grid>
</UserControl>