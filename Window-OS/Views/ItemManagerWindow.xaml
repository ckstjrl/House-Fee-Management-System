<Window x:Class="ManagementHouseFee.Views.ItemManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
        mc:Ignorable="d"
        Title="항목 관리" Height="400" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:ItemManagerViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" VerticalContentAlignment="Center" Padding="5" FontSize="14">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding AddCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <Button Grid.Column="1" Content="추가" Command="{Binding AddCommand}" Width="60" Margin="5,0,0,0"/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1">
            <ListBox ItemsSource="{Binding Items}" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsChecked}" 
                                  Content="{Binding Name}" 
                                  FontSize="16" 
                                  Margin="5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="선택 삭제" Command="{Binding DeleteCommand}" Width="80" Height="30" Background="#FF5555" Foreground="White" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</Window>