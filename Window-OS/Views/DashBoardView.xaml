<UserControl x:Class="ManagementHouseFee.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:ManagementHouseFee.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:DashboardViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F0F2F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="20" Background="White" CornerRadius="15" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="📊 최근 6개월 관리비 추이" FontSize="20" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="막대" VerticalAlignment="Center" Margin="5" Foreground="Gray"/>
                        <CheckBox IsChecked="{Binding IsLineChart}" VerticalAlignment="Center" ToolTip="체크하면 꺾은선, 해제하면 막대">
                            <TextBlock Text="꺾은선 보기"/>
                        </CheckBox>
                    </StackPanel>
                </Grid>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding ChartSeries}" LegendLocation="None">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding ChartLabels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis LabelFormatter="{Binding YAxisFormatter}" MinValue="0"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <StackPanel Grid.Column="1" Margin="0,20,20,20">

            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20" Height="150">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="지난달 관리비" FontSize="16" Foreground="Gray"/>
                        <TextBlock Text="{Binding LastMonthTotal}" FontSize="32" FontWeight="Bold" Foreground="#007ACC" Margin="0,10,0,0"/>
                        <TextBlock Text="이번 달은 얼마나 나올까요?" FontSize="12" Foreground="#AAA" Margin="0,5,0,0"/>
                    </StackPanel>
                    <TextBlock Text="📅" FontSize="40" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2"/>
                </Grid>
            </Border>

            <Border Background="White" CornerRadius="15" Padding="20" Height="150">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="작년 월평균" FontSize="16" Foreground="Gray"/>
                        <TextBlock Text="{Binding LastYearAverage}" FontSize="32" FontWeight="Bold" Foreground="#FF5722" Margin="0,10,0,0"/>
                        <TextBlock Text="1년 전 나의 소비 습관" FontSize="12" Foreground="#AAA" Margin="0,5,0,0"/>
                    </StackPanel>
                    <TextBlock Text="💰" FontSize="40" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2"/>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>